# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Env / secrets
.env
.env.local
cookies.txt
*.cookies

# OS
.DS_Store

# Large generated artifacts (the gzipped seed index IS committed via force-include;
# the raw uncompressed build is not)
scripts/_build/
*.db
!*.db.gz

# Harvest resume checkpoint (build artifact)
.harvest_checkpoint.json

# Faz 0 raw probe captures — multi-MB; kept on disk for deriving trimmed test
# fixtures + building facets.json, but not committed. Trimmed fixtures ARE committed.
tests/fixtures/faz0/search_keyword_islem4.html
tests/fixtures/faz0/search_ataturk.html
tests/fixtures/faz0/getAllABD.html
